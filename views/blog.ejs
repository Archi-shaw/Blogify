<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head') %>
    <title> Home </title>
</head>
<body>
    <%- include('./partials/nav') %>
     
    <div class="container mt-3">
        <h1> <%= blog.title %></h1>
        <img src="<%= blog.coverImage %>" width="300px" /> 
        <p> <%= blog.body %> </p>
    </div>
    <div class="m-3">
        <img src="/<%= blog.createdBy.profileImage %>" width="50px" /> <%= blog.createdBy.name %>
    </div>

     <% if(locals.user) {%>
        <div class="container m-3 ">
    <h3> Comments </h3>
    <form class="container mt-5" style="width: 500px;" action="/user/signin" method="POST">
    <div class="mb-3">
    <input type="text" name="content" class="form-control" id="exampleInputEmail1" placeholder="Enter your comment"
     aria-describedby="emailHelp">
    </div>
    <button type="submit" class="btn btn-primary">Submit</button>
   </form>
    </div> 
        <% } else {%>
        
        <%  } %>
    <%- include('./partials/script') %>
</body>
</html>